name: Render Video

on:
  workflow_dispatch:
    inputs:
      video_name:
        description: 'Video folder name'
        required: true
        default: 'groovy-commutator'
        type: string
      quality:
        description: 'Render quality'
        required: true
        default: 'low'
        type: choice
        options:
        - low
        - medium
        - high

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg texlive-full libcairo2-dev libpango1.0-dev
          pip install manim numpy
      
      - name: Render all scenes
        run: |
          VIDEO_NAME="${{ github.event.inputs.video_name }}"
          QUALITY="${{ github.event.inputs.quality }}"
          
          case "$QUALITY" in
            low) FLAGS="-ql" ;;
            medium) FLAGS="-qm" ;;
            high) FLAGS="-qh" ;;
          esac
          
          # Render all scenes non-interactively
          manim $FLAGS "videos/$VIDEO_NAME/script.py" --format=mp4 --write_all
      
      - name: Upload video
        uses: actions/upload-artifact@v4
        with:
          name: video-${{ github.event.inputs.video_name }}
          path: media/videos/**/*.mp4
